artui/src/lsp/defaults.toml — third-party attribution

The contents of `defaults.toml` in this directory are derived from
helix-editor/helix's `languages.toml` (https://github.com/helix-editor/helix),
which is licensed under the Mozilla Public License, Version 2.0 (MPL-2.0).

The TOML data is regenerated by `scripts/sync-helix-lsp.py`, which fetches
upstream `languages.toml` and re-emits the language-server section in artui's
flat `[server.<id>]` schema. Curated extensions / root-marker bindings for
servers helix does not bind to a language by default (e.g. pyright, deno,
ruff) are added by the script from the respective upstream projects'
documentation; see EXTRA_BINDINGS in `scripts/sync-helix-lsp.py` for the
complete list.

Per MPL-2.0 §3, this NOTICE file documents the origin of the data. The
TOML payload is data, not linked code, so the file-level copyleft of
MPL-2.0 §1.3 does not propagate to the rest of artui's source tree. We
attribute regardless, both as a courtesy and to keep the audit trail
clean.

The full text of MPL-2.0 is available at:
  https://www.mozilla.org/en-US/MPL/2.0/

Original copyright:
  Copyright (c) 2021–present The helix-editor contributors

User-supplied additions to the registry (server entries that do not appear
in helix's `languages.toml`) carry their own copyright and are not subject
to MPL-2.0.
